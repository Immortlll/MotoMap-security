# OpenClaw 安全过滤器依赖包
# 核心依赖
python>=3.8

# Web框架
flask>=2.0.0
flask-cors>=3.0.0
fastapi>=0.85.0
uvicorn>=0.18.0

# HTTP客户端
requests>=2.25.0
aiohttp>=3.8.0

# 数据验证
pydantic>=1.10.0

# 可视化依赖
matplotlib>=3.5.0
networkx>=2.6.0
numpy>=1.21.0

# 图像处理
pillow>=8.0.0

# 可选依赖（用于扩展功能）
# transformers>=4.20.0  # 用于NLP语义分析
# redis>=4.0.0          # 用于分布式缓存

# 开发和测试依赖
pytest>=7.0.0          # 单元测试
pytest-cov>=4.0.0      # 测试覆盖率
black>=22.0.0           # 代码格式化
flake8>=5.0.0           # 代码检查

# Docker和部署
gunicorn>=20.0.0        # 生产环境WSGI服务器
nginx>=1.0.0            # 负载均衡（通过Docker）
